local material = loadstring(game:HttpGet("https://raw.githubusercontent.com/HairBaconGamming/Gamming-Material-HUB/main/Main"))

local gui = material.Load(
	"Noob" -- title
)

local temp = gui.new(
	"Home" -- title
)

local button = temp.Button(
	"Click me", -- title
	function()
		print("Amogus")
	end -- callback
)

local toggle = temp.Toggle(
	"Click me", -- title
	function(Value)
		print(Value)
	end, -- callback
	false -- enable
)
